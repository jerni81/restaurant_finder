import React from "react";

const headerStyle = {
  display: "flex",
  justifyContent: "center",
  backgroundColor: "red"
};

function Header() {
  return <h1 style={headerStyle}>Whats for dinner?</h1>;
}

export default Header;
